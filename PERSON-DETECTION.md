# 人像检测说明

- 设置页中的“人像检测”默认关闭。
- 开启后，图片先保存为 `原文件名.扩展名.pending`，并把文件路径放入后台队列；队列不保存图片字节，因此不会因为下载速度快而大量占用内存。
- 下载线程只负责落盘和入队，不等待 YOLO 推理。一个作者的资源下载阶段结束后，可立即开始下一位作者；上一位作者继续在后台检测。
- 后台使用 `YoloDotNet 4.2.0` 和 CPU Execution Provider 单工作线程逐张检测作品图片及可选封面。
- 检测到 `person` 类别时将 `.pending` 文件改回正式文件名；未检测到人物时删除 `.pending` 文件；模型初始化、图片解码或推理失败时恢复正式文件名并保留图片，避免异常误删。
- 未开启人像检测时完全沿用普通下载路径，不会生成 `.pending` 文件。
- 程序异常退出后遗留的 `.pending` 文件会在下次启动时自动重新加入检测队列。
- CPU 推理会明显增加处理时间和算力占用；目标较小、遮挡严重、特殊画风或图片格式不受支持时可能误判。

## 放置模型

项目不自动下载或分发预训练模型。请准备一个使用 COCO 类别的目标检测 ONNX 模型。程序会按以下规则搜索：

1. 优先使用固定名称：

```text
person-detection.onnx
```

2. 如果不存在固定名称，则在 `Models` 文件夹中搜索 `yolo11?.onnx`。`?` 表示 `yolo11` 后可没有字母，或带任意一个字母，例如：

```text
yolo11.onnx
yolo11n.onnx
yolo11m.onnx
```

将模型放入任意一个位置：

1. 程序根目录下的 `Models` 文件夹；
2. 当前用户本地应用数据目录下的 `HelloCrab/Models` 文件夹。

除 `person-detection.onnx` 外，程序只会自动识别符合 `yolo11?.onnx` 规则的文件；不会误用 `yolo11n-seg.onnx`、`yolo11n-pose.onnx`、`yolo11n-cls.onnx` 等非普通目标检测模型。

## 模型许可

YoloDotNet 本身使用 MIT 许可证，但预训练 YOLO 模型可能采用其他许可证或需要商业许可。
使用者应根据自己的使用和分发场景确认模型来源及许可要求。
